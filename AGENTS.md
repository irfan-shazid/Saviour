# Expo HAS CHANGED

This project targets **Expo SDK 54** (React Native 0.81) — pinned there so the
Play Store build of Expo Go can open it.

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code. Do not follow newer-SDK guidance; APIs have moved between
releases.
